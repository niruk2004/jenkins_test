pipeline:
  agent:
    any:

  stages:
    - stage: "checkout"
      steps:
        - script:
            groovy: |
              checkout scm
    - stage: "run python app"
      steps:
        - sh "python3 pythonapp.py"

    - stage: "done"
      steps:
        - sh "echo 'Pipeline execution complete!'"
